import React from "react";

// CSS File
import "./teamPage-styles.css";

// Import teamInfo
import teamInfo from "./teaminfo.json";

export default function TeamInfo() {
    return (
        <div>
            {/* Map out teamInfo json */}
            {/* Put it into a row */}
            {/* First column should have the picture of the team member */}
            {/* If there is no picture show a pride flag instead */}
            {/* Second column will have the memeber's bio info */}
        </div>
    );
};